dependencies for development /use 
    pip
    pip install tk 
    pip install termcolor
    sudo apt-get install gcc-multilib 
    sudo apt-get install python3-tk  ?
    pip install python-dev-tools
    pip install PyQt6

    PySide6  ?

not implamented yet 
    add-auto-load-safe-path <path to project>/.gdbinit
    python3 -m pip install pip setuptools --upgrade (because PyQt6 is not nice)
    sudo python3 -m pip install PyQt6
to use 
make sure the binary executable was comblied with -g flag to enable full debugging functionality
run this command in a terminal
gdb ./executable -x programclass.py

info about where to put this found below <<TODO>>
Reading symbols from ./simple_program...
warning: File "/home/devsheep/Documents/ppgdb/pretty-python-gdb-stack/.gdbinit" auto-loading has been declined by your `auto-load safe-path' set to "$debugdir:$datadir/auto-load".
To enable execution of this file add
        add-auto-load-safe-path /home/devsheep/Documents/ppgdb/pretty-python-gdb-stack/.gdbinit
line to your configuration file "/home/devsheep/.config/gdb/gdbinit".
To completely disable this security protection add
        set auto-load safe-path /
line to your configuration file "/home/devsheep/.config/gdb/gdbinit".
For more information about this security protection see the
"Auto-loading safe path" section in the GDB manual.  E.g., run from the shell:
        info "(gdb)Auto-loading safe path"


currently only supports development in a linux enviornment 

using:

added commands:
<copy paste docstrings>





